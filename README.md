# iris-species-predictor-streamlit
A simple Streamlit web application that predicts the species of an Iris flower based on user-provided measurements. The app uses a RandomForestClassifier trained on the classic Iris dataset from scikit-learn. Users can input values for sepal length, sepal width, petal length, and petal width using interactive sliders
